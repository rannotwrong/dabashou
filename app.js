const $=s=>document.querySelector(s),esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const people=[
{id:1,name:'林小满',initial:'满',role:'周末摄影师',city:'上海',teach:'摄影',title:'把日常拍成电影',want:'Python',level:'进阶 · 3 年经验',cat:'生活艺术',mode:'线上 / 线下',time:'周末下午',desc:'从构图到调色，陪你拍出第一组有故事的照片。想用 Python 整理我的照片库。',outcome:'独立完成一组 9 张主题照片',lesson:'手机构图练习 → 光线与色彩 → 作品复盘'},
{id:2,name:'陈一舟',initial:'舟',role:'独立音乐爱好者',city:'杭州',teach:'吉他',title:'弹唱你的第一首歌',want:'Python',level:'进阶 · 5 年经验',cat:'音乐表达',mode:'线上',time:'工作日晚上',desc:'不用先啃乐理，从你喜欢的一首歌开始。希望学会写点小脚本，告别重复工作。',outcome:'完整弹唱一首入门歌曲',lesson:'认识和弦 → 节奏练习 → 完整弹唱'},
{id:3,name:'许可',initial:'可',role:'产品设计师',city:'上海',teach:'Figma',title:'把脑海里的想法画出来',want:'英语口语',level:'进阶 · 4 年经验',cat:'职场技能',mode:'线上',time:'周末下午',desc:'一起从零做一个好用的小界面。想练习产品分享时的英文表达，互相当彼此的搭子。',outcome:'完成一套可点击的 App 原型',lesson:'布局与组件 → 交互连线 → 原型走查'},
{id:4,name:'周南',initial:'南',role:'城市生活记录者',city:'北京',teach:'英语口语',title:'开口，比完美更重要',want:'摄影',level:'熟练 · 日常交流',cat:'语言交流',mode:'线上',time:'工作日晚上',desc:'用真实生活场景练口语，不背枯燥的句子。想学拍照，让旅行记忆更生动。',outcome:'完成 10 分钟英文旅行对话',lesson:'场景表达 → 角色扮演 → 纠音复盘'},
{id:5,name:'何木',initial:'木',role:'数据分析师',city:'深圳',teach:'Python',title:'让电脑帮你做重复的事',want:'吉他',level:'进阶 · 4 年经验',cat:'编程技术',mode:'线上',time:'工作日晚上',desc:'从 Excel 自动整理开始，带你跑通第一个脚本。也想在下班后，弹一首喜欢的歌。',outcome:'完成一个自动整理表格的脚本',lesson:'基础语法 → 表格处理 → 实战运行'},
{id:6,name:'苏晴',initial:'晴',role:'一人食研究员',city:'上海',teach:'烹饪',title:'认真吃好每一顿饭',want:'摄影',level:'熟练 · 家常菜',cat:'生活艺术',mode:'线下',time:'周末下午',desc:'三道简单又好吃的拿手菜，让下班后的厨房不再是难题。想学拍出食物的温度。',outcome:'独立完成两菜一汤',lesson:'备菜技巧 → 火候练习 → 独立复刻'}];
let saved;try{saved=JSON.parse(localStorage.getItem('dabashou-v1')||'null')}catch{}
let state={profile:{name:'新朋友',teach:'Python',want:'摄影',time:'周末下午',mode:'线上',goal:'拍出一组有故事的日常照片'},exchanges:[],published:false,...saved};let view='discover',category='全部',query='',matchOnly=false,mode='全部方式';
function persist(){try{localStorage.setItem('dabashou-v1',JSON.stringify(state))}catch{toast('浏览器暂时无法保存，当前页面仍可继续体验')}}
function toast(t){$('#toast').textContent=t;$('#toast').classList.add('visible');clearTimeout(window.tt);window.tt=setTimeout(()=>$('#toast').classList.remove('visible'),2800)}
function score(p){return (p.teach===state.profile.want?40:0)+(p.want===state.profile.teach?40:0)+(p.time===state.profile.time?10:0)+(p.mode.includes(state.profile.mode)?10:0)}
function dual(p){return p.teach===state.profile.want&&p.want===state.profile.teach}
function render(){document.querySelectorAll('.nav').forEach(n=>n.classList.toggle('active',n.dataset.view===view));$('#count').textContent=state.exchanges.length||'';if(view==='discover')discover();if(view==='profile')profile();if(view==='exchanges')exchanges()}
function discover(){const p=state.profile;$('#app').innerHTML=`<section class="intro"><div><h1>用你会的，换你想学的。</h1><p>找到技能互补的伙伴，从一次小交换开始。</p></div></section><section class="profile-strip" aria-label="我的匹配条件"><div class="avatar">我</div><div><strong>我的技能名片</strong><span class="muted" style="font-size:11px">${state.published?'已发布到本机演示':'先试试看，找到你的第一位伙伴'}</span></div><div class="divider"></div><div><span class="muted" style="font-size:12px">我能教</span> <span class="tag">${esc(p.teach)}</span></div><span class="muted">⇄</span><div><span class="muted" style="font-size:12px">我想学</span> <span class="tag">${esc(p.want)}</span></div><button class="textbtn" id="editprofile">编辑偏好 ›</button></section><div class="workspace"><section><div class="toolbar"><label class="search"><span aria-hidden="true">⌕</span><input id="search" aria-label="搜索技能或伙伴" placeholder="试试「摄影」「吉他」或伙伴名字" value="${esc(query)}"></label><select id="mode" aria-label="学习方式"><option>全部方式</option><option>线上</option><option>线下</option></select></div><div class="chips" role="group" aria-label="技能分类">${['全部','生活艺术','编程技术','音乐表达','语言交流','职场技能'].map(c=>`<button class="chip ${category===c?'active':''}" data-cat="${c}">${c}</button>`).join('')}</div><div class="resultline"><strong>为你发现 <span id="resultcount"></span></strong><label><input type="checkbox" id="dual" ${matchOnly?'checked':''}> 只看双向匹配</label></div><div class="cards" id="cards"></div></section></div>`;$('#mode').value=mode;$('#search').oninput=e=>{query=e.target.value;cards()};$('#mode').onchange=e=>{mode=e.target.value;cards()};$('#dual').onchange=e=>{matchOnly=e.target.checked;cards()};document.querySelectorAll('[data-cat]').forEach(b=>b.onclick=()=>{category=b.dataset.cat;discover()});$('#editprofile').onclick=()=>editProfile(false);cards()}
function cards(){let list=people.filter(p=>(category==='全部'||p.cat===category)&&(mode==='全部方式'||p.mode.includes(mode))&&(!matchOnly||dual(p))&&(!query||[p.name,p.teach,p.want,p.desc].join(' ').toLowerCase().includes(query.toLowerCase()))).sort((a,b)=>score(b)-score(a));$('#resultcount').textContent=`· ${list.length} 位伙伴`;$('#cards').innerHTML=list.length?list.map(p=>`<article class="card"><div class="cardtop"><div class="avatar">${p.initial}</div><div><strong>${p.name}</strong><div class="person-meta">${p.city} · ${p.role}</div></div><span class="match ${dual(p)?'is-match':''}">${dual(p)?'双向匹配':score(p)>=40?'有共同方向':'探索新可能'}</span></div><div class="skillpair"><div class="skillrow"><span>能教</span><strong>${p.teach}</strong><span style="width:auto;letter-spacing:0;font-size:10px">${p.level.split(' · ')[0]}</span></div><div class="skillrow"><span>想学</span><strong>${p.want}</strong></div></div><p>${p.desc}</p><div class="meta"><span>${p.time}</span><span>${p.mode}</span></div><div class="cardbottom"><button class="textbtn" data-detail="${p.id}">了解技能 ›</button><button class="primary" data-invite="${p.id}">${state.exchanges.some(e=>e.person===p.id)?'查看交换':'发起交换'}</button></div></article>`).join(''):`<div class="empty"><h2>暂时没有合适的伙伴</h2><p>试试其他技能，或放宽学习方式和匹配条件。</p><button class="secondary" id="clear">重置筛选</button></div>`;document.querySelectorAll('[data-detail]').forEach(b=>b.onclick=()=>detail(+b.dataset.detail));document.querySelectorAll('[data-invite]').forEach(b=>b.onclick=()=>invite(+b.dataset.invite));if($('#clear'))$('#clear').onclick=()=>{query='';mode='全部方式';matchOnly=false;category='全部';discover()}}
function modal(title,body){$('#modal').innerHTML=`<div class="modalbody"><div class="modalhead"><h2 id="dialog-title">${title}</h2><button class="close" aria-label="关闭">×</button></div>${body}</div>`;$('#modal .close').onclick=()=>$('#modal').close();$('#modal').showModal()}
function opts(items,value){return items.map(x=>`<option ${x===value?'selected':''}>${esc(x)}</option>`).join('')}
function editProfile(publish){const p=state.profile;modal(publish?'把你的擅长，分享出来':'让匹配更懂你',`<p class="muted">不用是专家。能带一个人迈出第一步，就值得分享。</p><form id="profileform"><label class="field">怎么称呼你<input name="name" required maxlength="16" value="${esc(p.name)}" placeholder="你的昵称"></label><div class="formgrid"><label class="field">我能教<select name="teach">${opts(['Python','摄影','吉他','Figma','英语口语','烹饪'],p.teach)}</select></label><label class="field">我想学<select name="want">${opts(['摄影','Python','吉他','Figma','英语口语','烹饪'],p.want)}</select></label></div><div class="formgrid"><label class="field">方便的时间<select name="time">${opts(['周末下午','工作日晚上'],p.time)}</select></label><label class="field">偏好的方式<select name="mode">${opts(['线上','线下'],p.mode)}</select></label></div><label class="field">我的小目标<textarea name="goal" required maxlength="120" placeholder="例如：独立弹唱一首喜欢的歌">${esc(p.goal)}</textarea></label><div class="notice">演示无需注册。技能名片与交换记录仅保存在当前浏览器，不会对其他访客公开。</div><button class="primary full">${publish?'发布名片，看看谁合拍':'保存偏好，重新匹配'} →</button></form>`);$('#profileform').onsubmit=e=>{e.preventDefault();let next=Object.fromEntries(new FormData(e.target));next.name=next.name.trim();next.goal=next.goal.trim();if(!next.name||!next.goal)return toast('请填写昵称和学习目标');if(next.teach===next.want)return toast('试试用一种擅长，交换另一种技能');state.profile=next;if(publish)state.published=true;persist();$('#modal').close();view='discover';render();toast(publish?'技能名片已发布到本机演示，匹配已更新':'偏好已保存，匹配已更新')}}
function detail(id){const p=people.find(p=>p.id===id);modal(p.title,`<div class="cardtop"><div class="avatar">${p.initial}</div><div><strong>${p.name}</strong><div class="person-meta">${p.role} · ${p.level}</div></div></div><p>${p.desc}</p><h3>这次能学会什么</h3><p>${p.outcome}</p><div class="notice">${p.lesson}</div><h3>为什么推荐给你</h3><p class="muted">${p.teach===state.profile.want?'✓ TA 会教你想学的技能':'○ TA 的技能与你当前目标不同'}<br>${p.want===state.profile.teach?'✓ 你会的正是 TA 想学的':'○ 你能教的与 TA 需求不同，建议先沟通'}<br>${p.time===state.profile.time?'✓ 你们的空闲时间一致':'○ 需要再协调空闲时间'}<br>${p.mode.includes(state.profile.mode)?'✓ 支持你偏好的学习方式':'○ 需要再协调学习方式'}</p><button class="primary full" id="detailinvite">一起试着交换 ⇄</button>`);$('#detailinvite').onclick=()=>{$('#modal').close();invite(id)}}
function invite(id){if(state.exchanges.some(e=>e.person===id)){view='exchanges';render();return}const p=people.find(p=>p.id===id);modal(`和${p.name}开启第一次交换`,`<div class="notice">你教 ${esc(state.profile.teach)} ⇄ TA 教 ${p.teach}<br>${dual(p)?'你们的技能刚好双向匹配，先约一节轻松的体验课。':'你们尚未双向匹配，可以先发起意向，沟通彼此的目标。'}</div><form id="inviteform"><label class="field">想约在什么时候<select name="time">${opts(['周末下午','工作日晚上'],state.profile.time)}</select></label><div class="formgrid"><label class="field">交换方式<select name="mode">${opts(['线上','线下'],state.profile.mode)}</select></label><label class="field">每人教学时长<select name="duration"><option>30 分钟</option><option>45 分钟</option><option>60 分钟</option></select></label></div><label class="field">给 TA 的第一句话<textarea name="message" required maxlength="200">你好！我可以教你${esc(state.profile.teach)}，想向你学习${p.teach}。我们先一起完成一个小目标吧。</textarea></label><p class="muted" style="font-size:11px">这是模拟邀请，不会向真实用户发送消息。下一步可体验接受邀请与学习计划。</p><button class="primary full">创建交换邀请 →</button></form>`);$('#inviteform').onsubmit=e=>{e.preventDefault();const data=Object.fromEntries(new FormData(e.target));if(!data.message.trim())return toast('请写一句邀请说明');state.exchanges.unshift({id:Date.now(),person:id,teach:state.profile.teach,want:p.teach,...data,status:'pending',done:[false,false,false]});persist();$('#modal').close();view='exchanges';render();toast('模拟邀请已创建，去看看你的交换计划')}}
function exchanges(){$('#app').innerHTML=`<section class="intro"><div><h1>我的交换。</h1><p>一次只定一个小目标，和伙伴一起走到下一步。</p></div><button class="secondary" id="find">继续发现伙伴 ↗</button></section>${state.exchanges.length?state.exchanges.map(e=>{const p=people.find(p=>p.id===e.person),n=e.done.filter(Boolean).length;return `<article class="plan"><div class="planhead"><div><span class="badge">${e.status==='pending'?'等待对方回应 · 演示':n===3?'本次交换已完成':'交换进行中'}</span><h3>你教 ${esc(e.teach)} <span class="muted">⇄</span> ${p.name}教 ${p.teach}</h3><span class="muted" style="font-size:12px">${esc(e.time)} · ${esc(e.mode)} · 每人 ${esc(e.duration)}</span></div><div class="avatar">${p.initial}</div></div><p class="muted" style="font-size:13px">“${esc(e.message)}”</p>${e.status==='pending'?`<div class="notice">邀请已记录。真实产品中由对方确认；在这里可以模拟对方接受，体验接下来的学习流程。</div><button class="primary" style="margin-top:18px" data-accept="${e.id}">模拟对方接受 →</button> <button class="textbtn" data-cancel="${e.id}">撤回邀请</button>`:`<div class="progress" aria-label="学习进度 ${n}/3"><div style="width:${n/3*100}%"></div></div><div class="checklist">${['一起确认目标：'+p.outcome,'完成第一轮：你教 '+e.teach+'，TA 教 '+p.teach,'互相反馈，记录收获并完成本次交换'].map((x,i)=>`<label><input type="checkbox" data-check="${e.id}" data-index="${i}" ${e.done[i]?'checked':''}>${esc(x)}</label>`).join('')}</div><span class="muted" style="font-size:12px">${n}/3 已完成${n===3?' · 很棒，又多了一项小擅长！':' · 按自己的节奏来就好'}</span>`}</article>`}).join(''):`<div class="empty"><div style="font-size:40px">⇄</div><h2>第一份交换，从打个招呼开始</h2><p>挑一位合拍的伙伴，试着发出你的第一份邀请。</p><button class="primary" id="emptyfind">去发现伙伴 →</button></div>`}`;$('#find').onclick=()=>{view='discover';render()};if($('#emptyfind'))$('#emptyfind').onclick=$('#find').onclick;document.querySelectorAll('[data-accept]').forEach(b=>b.onclick=()=>{state.exchanges.find(e=>e.id===+b.dataset.accept).status='active';persist();render();toast('模拟接受成功，你们的学习计划已开启')});document.querySelectorAll('[data-cancel]').forEach(b=>b.onclick=()=>{state.exchanges=state.exchanges.filter(e=>e.id!==+b.dataset.cancel);persist();render();toast('邀请已撤回')});document.querySelectorAll('[data-check]').forEach(b=>b.onchange=()=>{state.exchanges.find(e=>e.id===+b.dataset.check).done[+b.dataset.index]=b.checked;persist();render()})}
function profile(){const p=state.profile;$('#app').innerHTML=`<section class="intro"><div><h1>我的技能。</h1><p>具体一点的小目标，更容易遇见对的人。</p></div></section><section class="profilepanel"><span class="badge">${state.published?'名片已发布 · 本机演示':'体验名片 · 尚未发布'}</span><h2 style="margin-top:20px">${esc(p.name)}的技能名片</h2><div class="skillpair"><div class="skillrow"><span>我能教</span><strong>${esc(p.teach)}</strong></div><div class="skillrow"><span>我想学</span><strong>${esc(p.want)}</strong></div></div><p>学习目标：${esc(p.goal)}</p><p class="muted">${esc(p.time)} · ${esc(p.mode)}</p><button class="primary" id="profileedit">${state.published?'编辑技能名片':'完善并发布名片'} →</button><p class="muted" style="font-size:11px">演示数据仅保存在当前浏览器，可以随时调整。</p></section>`;$('#profileedit').onclick=()=>editProfile(true)}
document.querySelectorAll('.nav').forEach(b=>b.onclick=()=>{view=b.dataset.view;render()});$('#publish').onclick=()=>editProfile(true);$('.brand').onclick=e=>{e.preventDefault();view='discover';render()};$('#modal').onclick=e=>{if(e.target===$('#modal')){const r=$('#modal').getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)$('#modal').close()}};render();
if(document.modelContext?.registerTool){try{Promise.resolve(document.modelContext.registerTool({name:'search_skill_partners',description:'筛选演示伙伴并在发现页显示结果，不发送邀请。',inputSchema:{type:'object',properties:{query:{type:'string'}},required:['query'],additionalProperties:false},annotations:{readOnlyHint:true},execute(input){if(!input||typeof input.query!=='string'||input.query.length>100)throw new Error('query must be a string of up to 100 characters');query=input.query;category='全部';mode='全部方式';matchOnly=false;view='discover';render();return {count:$('#cards').querySelectorAll('.card').length,query}}})).catch(()=>{})}catch{}}
